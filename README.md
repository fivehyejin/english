# English Learning Notebook

영어 회화 학원 수업 내용을 **노트처럼 깔끔하게 정리해서 다시 꺼내 볼 수 있게** 해주는 웹앱. 복습 관리 앱이 아니라, 잘 정돈된 **요약 노트**.

## 목차
1. [제품 개요](#1-제품-개요)
2. [핵심 디자인 원칙](#2-핵심-디자인-원칙)
3. [화면 구조](#3-화면-구조)
4. [데이터 모델](#4-데이터-모델)
5. [기능 명세](#5-기능-명세)
6. [기술 스택](#6-기술-스택)
7. [개발 우선순위](#7-개발-우선순위)
8. [반응형](#8-반응형)

---

## 1. 제품 개요

### 문제
영어 학원에서 매달 수십~수백 개의 예문과 패턴을 배운다. 종이 교재와 노트는 누적되면서 **"이날 뭐 배웠더라?"**가 잘 안 보인다. 찾으려 들어가도 정보가 평면적으로 나열돼 있어 핵심과 디테일이 구분이 안 된다.

### 해결
- **목차 → 날짜(Day) → 내용** 3단 구조로, 각 Day가 하나의 "노트 페이지"처럼 보이게.
- 각 Day 상단에 "이 날의 핵심"을 한눈에 파악할 수 있는 **요약 박스**.
- 수업 중 적어둔 필기(교재 주석 + 별도 종이 필기)를 원래 배운 내용과 **자연스럽게 통합**.

### 이 제품이 아닌 것 (명시적 비목표)
- ❌ 망각곡선 복습 관리 앱
- ❌ 플래시카드 시험 앱
- ❌ 북마크·즐겨찾기 앱
- ❌ Duolingo 같은 게이미피케이션
- ❌ SNS·공유 기능

### 사용자
혜진 (1인 사용자). 영어 회화 학원 초급 2개월차 · 10개월 Catch up 과정. 업무는 DMS UI/UX 기획. 주 사용 기기는 **iPhone (모바일 세로 모드)** + 데스크탑(Mac).

### 사용 시나리오
- **수업 후**: 앱 열어서 오늘 배운 Day를 펼쳐보며 정리
- **이동 중**: 최근 Day 스크롤하며 머릿속에 다시 훑기
- **수업 전**: 이전 Day의 "이 날의 핵심"만 빠르게 확인

---

## 2. 핵심 디자인 원칙

### 원칙 1: 열자마자 '오늘 뭐 배웠는지'가 보인다
홈 화면은 Day 목차 형태. 각 Day 카드에 **주제 + 핵심 포인트 한 줄 + 그룹 개수 + 예문 수**가 바로 보여야 함.

### 원칙 2: Day 안으로 들어가면 '노트 한 페이지'처럼 쭉 보인다
- 그룹 카드 펼치기·접기 **없음**. 처음부터 다 열려있음.
- 스크롤 한 번으로 그 날 배운 모든 예문과 필기가 다 보임.
- 상단에 "이 날의 핵심" 요약 박스 (눈에 띄게).
- 수업노트는 **별도 강조 박스**로 구분 (진짜 필기처럼).

### 원칙 3: 정보에 강약이 있다
| 계층 | 시각적 처리 |
|---|---|
| **이 날의 핵심** (headline) | 페이지 상단, 큼직한 문장 |
| **Key Points** (핵심 포인트) | 불릿 리스트, 굵은 강조 |
| **수업노트 박스** | 배경색 + 아이콘 (노트 느낌) |
| **그룹 타이틀** (패턴명) | 큰 헤더 + 패턴 구조 병기 |
| **예문** | 영어(크게) → 한국어(작게) |
| **예문 필기** | 예문 아래 ✏️ 작게 |

### 원칙 4: 관리 기능은 숨긴다
복습 체크, 북마크, 플래시카드 같은 건 이 제품의 핵심이 아니다. 있어야 한다면 설정 메뉴 뒤로.

---

## 3. 화면 구조

### 3.1 홈 (Day 목차)

```
┌──────────────────────────────────────────┐
│  English Notebook                  [🌓]  │
│  Month 2 · Catch up · 구문 학습           │
│                                          │
│  📍 최근 학습: Day 4 · 2일 전             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  Day 4 · TAKE + TRY                      │
│  ─────────────────────────────────────    │
│  💡 TAKE는 '내 쪽으로 가져옴'             │
│     TRY는 뒤에 뭐 오느냐로 의미가 달라짐   │
│                                          │
│  6 그룹 · 70 예문 · ✏️ 수업노트 2개       │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  Day 3 · HAVE · GET · KEEP               │
│  ─────────────────────────────────────    │
│  💡 상태를 뒤에 붙이는 3개 동사           │
│     태도가 다르다 (정적/동작/유지)        │
│                                          │
│  4 그룹 · 72 예문 · ✏️ 수업노트 1개       │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  Day 2 · 조동사 (modal verbs)            │
│  (... 생략)                              │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  Day 1 · 자·타동사로 모두 쓰이는 동사들   │
│  (... 생략)                              │
└──────────────────────────────────────────┘

     [ Month 1 보기 ]  [ Month 3 보기 ]    ← 향후 추가 시
```

### 3.2 Day 상세 (노트 페이지)

```
┌──────────────────────────────────────────┐
│  ← 목차로                        [🌓]    │
│                                          │
│  DAY 3                                   │
│  HAVE · GET · KEEP                       │
│  ═══════════════════════════════════════  │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ 💡 이 날의 핵심                           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│ 상태를 뒤에 붙이는 3개 동사 —            │
│ 태도가 다르다.                           │
│                                          │
│ • HAVE = 정적 소유 (이미 그 상태)         │
│ • GET  = 동작 (상태를 만듦)               │
│ • KEEP = 유지 (그 상태를 지킴)            │
│ • 같은 'door open'이라도 동사 선택으로     │
│   의미가 완전히 달라진다                  │
│                                          │
│ 📌 꼭 기억할 것                           │
│ • get the door open ≠ open the door      │
│   (get은 '어떻게든' 뉘앙스)               │
└──────────────────────────────────────────┘

┌ ✏️ 수업노트 · 04/14 ─────────────────────┐
│ 소유 4동사 비교                          │
│                                          │
│ • HAVE = 정적인 소유                      │
│ • GET  = 동적인 소유 + put                │
│ • KEEP = 지켜내는 소유                    │
│ • TAKE = 공격적 소유                      │
│                                          │
│ 공통 구조:                                │
│ S + [have/get/keep/take] + O +            │
│     [adv / p+n / adj / -ed / -ing]        │
│                                          │
│ (... 전체 필기 내용 쭉)                   │
└──────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▌ HAVE                                    [🔊]
  확보되어 있는 상태 — 정적인 소유
  “~한 상태로 가지고 있다”
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  We have the tent up now.            [🔊]
  우린 텐트 다 세워놨어.

  They always have the light on.      [🔊]
  주차장엔 늘 불을 켜놔.

  (... 33개 예문 이어짐)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▌ GET                                     [🔊]
  동작 (그 상태로 만들기) — 움직임·변동
  “~한 상태가 되게 하다”
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Get the tent up.                    [🔊]
  텐트 세워.

  (... 쭉)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▌ KEEP                                    [🔊]
  자제, 억제 — 반대로 움직이려는 걸 막는 동작
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  (... 쭉)

┌──────────────────────────────────────────┐
│              ← Day 2     Day 4 →         │
└──────────────────────────────────────────┘
```

### 3.3 네비게이션

- 홈에서 Day 카드 클릭 → Day 페이지
- Day 페이지 상단 `← 목차로` → 홈
- Day 페이지 하단 `← Day 2` / `Day 4 →` → 이전/다음 Day
- 향후 Month 늘어나면 홈에 Month 선택 추가

### 3.4 삭제된 화면
- ❌ Due Today 모드
- ❌ Starred 모드
- ❌ Exam Mode
- ❌ 헷갈리는 것 비교 모드 (요약에 자연스럽게 녹여서 대신)
- ❌ 진도표 (10개월 그리드)
- ❌ 복습 체크 버튼

---

## 4. 데이터 모델

자세한 타입은 `DATA_SCHEMA.md` 참고.

### 간단 요약

```typescript
interface CurriculumData {
  month: number;
  level: 'beginner' | 'intermediate';
  topic: string;
  topicNote?: string;
  daySummaries: Record<number, DaySummary>; // ← 새로 추가
  groups: PatternGroup[];
}

interface DaySummary {              // ← 새로 추가. Day 요약용
  dayTitle: string;                 // e.g. "HAVE · GET · KEEP"
  headline: string;                 // 이 날의 핵심 한 문장
  keyPoints: string[];              // 핵심 포인트 불릿 (markdown 강조 가능)
  mustRemember: string[];           // 꼭 기억할 것
}

interface PatternGroup {
  id: string;
  day: number;
  dayTopic: string;
  title: string;
  pattern?: string;
  meaning?: string;
  examples: Example[];
  kind?: 'class-note';     // 수업노트 전용 그룹
  notes?: string[];        // 그룹 레벨 필기
  noteSource?: string;     // 필기 출처 표시
}

interface Example {
  en: string;
  ko: string;
  note?: string;           // 예문별 필기
}
```

**사용자 상태는 거의 없다** (복습 관리 제거):

```typescript
interface AppState {
  lastViewedDay: { month: number; day: number } | null;  // "이어서 보기"
  theme: 'light' | 'dark';                                // 테마 설정
}
```

---

## 5. 기능 명세

### 5.1 홈 화면
- Month 정보 (topic, topicNote) 상단 고정
- 최근 본 Day 하이라이트 (`lastViewedDay` 기반, "📍 최근 학습" 배지)
- 각 Day 카드에 `daySummary.headline` 1~2줄 + 그룹/예문 개수 표시
- Day 카드 클릭 시 Day 페이지로 이동
- Day 정렬 역순 (최신 Day가 위)

### 5.2 Day 상세 페이지
- **헤더**: Day 번호, 주제, 목차로 돌아가기
- **이 날의 핵심 박스**: `daySummary.headline` (굵게) + `keyPoints` 리스트 + `mustRemember` (있으면 📌 아이콘 + 강조 배경)
- **수업노트 박스**: `kind === 'class-note'` 그룹들은 별도 박스로 분리해서 Day 상단 (핵심 박스 바로 아래)에 배치
  - 수업노트 박스 배경은 노트 종이 느낌 (옅은 색 + 좌측 세로선)
  - `noteSource`를 박스 타이틀에 표시 (e.g. "✏️ 수업노트 · 04/14")
- **일반 그룹들**: 순서대로 나열, 모두 펼쳐진 상태
  - 그룹 섹션 헤더: 큰 타이틀 + 패턴 + 의미
  - 그룹 레벨 필기 (`group.notes`)가 있으면 타이틀 아래 노트 박스로
  - 예문 리스트: 영어(크게 · Pretendard semibold) + 한국어(작게 · muted) + 예문 필기(✏️ 아이콘 + 옅은 배경)
  - 각 예문 우측에 🔊 발음 버튼
  - 그룹 섹션 상단에 그룹 전체 듣기 🔊 버튼 (첫 3개 예문)
- **하단 네비게이션**: 이전/다음 Day 링크

### 5.3 음성 재생 (Web Speech API)
- 예문별 🔊: 해당 영어 단독 재생
- 그룹 🔊: 첫 3개 예문 순차 재생 (`utterance.onend` 체이닝)
- `speechSynthesis.cancel()` 후 시작
- `utterance.lang = 'en-US'`, `utterance.rate = 0.9`
- 브라우저 미지원 시 `sonner` 토스트로 알림

### 5.4 라이트/다크 모드
- `next-themes` 사용
- 헤더 우측 상단 `Sun`/`Moon` 아이콘 토글
- 시스템 설정 따라가기가 기본

### 5.5 저장되는 것
- `lastViewedDay`: 마지막으로 본 Day (홈 "📍 최근 학습"에 표시용)
- `theme`: 라이트/다크 설정
- **그게 전부**. 복습 진행도·북마크 등 없음.

### 5.6 연습 모드 (Practice Mode) — v2.1 추가

노트를 읽는 것과 별개로, **패턴을 구분하는 감각을 키우기 위한** 별도 모드. 개별 예문을 외우는 게 아니라 **"이 상황엔 have를 써야 할까 keep일까?"** 같은 판단을 훈련.

#### 진입 방식
- Day 페이지 상단에 `[📝 연습 모드]` 버튼
- 클릭 시 `/day/[day]/practice` 이동
- Day 노트 페이지 자체는 건드리지 않음 (버튼 하나만 추가)

#### 시작 전 선택 화면

```
DAY 3 · 연습 모드

이 Day에 맞는 유형:
  ○ 빈칸 채우기 (HAVE/GET/KEEP 중 고르기)       72문제
  ○ 동사 고르기 (패턴을 먼저 인식)              72문제
  ○ 자·타동사 분류                              (Day 1 전용, 여기선 숨김)

세션 길이:
  ○ 5개   ○ 10개   ○ 전체

마지막 세션: 2일 전 · 3개 어려움 표시됨
[ 어려움 표시된 것만 풀기 (3문제) ]

[← Day 노트로]       [연습 시작 →]
```

#### 테스트 유형 (3가지)

**1. 빈칸 채우기 (Fill-in)** — 구분 학습의 핵심
```
"우린 늘 그 창문은 열어놔"

We always ___ the window open.

[ have ]  [ get ]  [ keep ]
```
- 선택지는 해당 Day의 자매 그룹에서 자동 생성
- Day 3 → have/get/keep, Day 4 → try N/try ~ing/try to V, Day 2 → could/might 등

**2. 동사 고르기 (Pattern Choice)** — 패턴 인식 먼저
```
"이 피아노 한번 쳐보세요"

어떤 try를 써야 할까요?
[ try + 명사 ]  [ try + ~ing ]  [ try + to V ]

→ 답하면 전체 문장 공개
```
- 문법 패턴을 먼저 골라야 하기 때문에 "왜 이 동사인지"를 이해시킴

**3. 자·타동사 분류 (Sort)** — Day 1 전용
```
"laugh"는 자동사? 타동사? 둘 다?

[자동사]  [타동사]  [둘 다]

힌트: 목적어를 바로 받나?
```
- Day 1에서만 제공. 다른 Day엔 숨김

#### 세션 중 화면

```
┌──────────────────────────────────────┐
│  Day 3 · 빈칸 채우기       4 / 10     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                      │
│  "우린 늘 그 창문은 열어놔"             │
│                                      │
│  We always ___ the window open.      │
│                                      │
│    [ have ]   [ get ]   [ keep ]     │
│                                      │
│  [🔊 정답 듣기]                       │
│                                      │
│  ○ 어려움 표시          [다음 →]       │
└──────────────────────────────────────┘
```

- 정답 클릭 시: 초록 피드백 + 자동 🔊 재생 + 0.8초 후 다음
- 오답 클릭 시: 빨간 피드백 + 정답 강조 + `🔊 듣기` 버튼 표시 + 사용자가 "다음" 클릭해야 진행
- "어려움 표시" 체크박스: 세션 끝날 때까지 남음

#### 세션 종료 화면

```
Day 3 · 연습 완료!

맞음: 7 / 10
어려움 표시: 3개 (다음 세션에 우선 노출)

3개 틀림:
  • We always ___ the window open.    (have)
  • Keep the door ___.                 (closed)
  • Get some ___ in your face.         (wind)

[다시 하기]   [Day 노트로]
```

#### 어려움 표시 (노트에도 조용히 표시)

Day 노트 페이지에서 "어려움 표시된 예문" 왼쪽에 **조용한 점 하나**:
```
  We always have the window open.
  우린 늘 그 창문은 열어놔.                  ●
```
- 점 색: `muted-foreground` (회색), 레드 금지 — **노트 소란스러워지면 컨셉 훼손**
- 점 클릭 시 수동으로 제거 가능 (툴팁: "어려움 표시 해제")
- 다음 세션에서 맞추면 자동 제거

#### ⚠️ 절대 하지 말 것 (복습 앱 회귀 방지)

| 금지 | 대신 |
|---|---|
| 망각곡선 일정 관리 | 사용자가 원할 때만 세션 시작 |
| "오늘 할당량 N개" | 세션 길이 매번 사용자 선택 |
| 홈에 "Due 3개" 같은 뱃지 | 홈은 그대로, 연습 모드는 Day 내부에서만 |
| "마스터됨" 상태 | 어려움 플래그가 자연 소멸 (맞추면 제거) |
| 복습 스테이지 1~5단계 | 단계 없음. 플래그만 존재/비존재 |
| 성취 게이미피케이션 (streak, 점수) | 세션 끝나면 결과만 표시, 누적 X |
| 북마크 ⭐ 기능 재도입 | "어려움 표시"는 자동이지 사용자가 찍는 게 아님 |

#### 저장 데이터

```typescript
interface PracticeState {
  difficultExamples: {
    [exampleKey: string]: {  // `${groupId}:${exampleIdx}`
      markedAt: string;       // ISO date
      sessionsStruggled: number;
    }
  };
  // 세션별 기록은 저장 X (익명 세션)
  // 통계·진도율 저장 X
}
```

#### 제거된 기능 (명시)
- ❌ 복습 체크 버튼
- ❌ 망각곡선 계산 · Due Today 리스트
- ❌ 예문 북마크 (⭐)
- ❌ Exam Mode (기존 플래시카드)
- ❌ 헷갈리는 것 비교 독립 탭 (요약에 녹임)
- ❌ 10개월 진도표 그리드
- ❌ 커스텀 그룹 직접 추가
- ❌ Export/Import JSON
- ❌ 연습 모드의 누적 통계 · 점수 · streak

### 5.7 선택 기능 (나중에)
아래는 MVP에 포함 안 함:
- 검색 (Day 내에서 Ctrl+F로 커버 가능)
- Month 선택 드롭다운 (지금은 Month 2만)

---

## 6. 기술 스택

자세한 디자인 가이드는 `DESIGN_GUIDE.md` 참고.

- **Next.js 14+ (App Router)** + **TypeScript**
- **Tailwind CSS** + **shadcn/ui**
- **Pretendard Variable** 폰트 (한영 통합)
- **lucide-react** 아이콘
- **sonner** 토스트
- **next-themes** 라이트/다크
- **localStorage** (아주 최소한의 상태만)
- **Web Speech API** (발음)
- 배포: **Vercel**

---

## 7. 개발 우선순위

### Phase 1 (MVP) — 노트 기능
1. 프로젝트 셋업 (Next.js + Tailwind + shadcn init)
2. Pretendard + Navy/Silver/Red 팔레트 적용 (`DESIGN_GUIDE.md` 참고)
3. `curriculum-m2.json` 로드 + 타입 정의
4. 홈 화면 (Day 목차)
5. Day 상세 페이지 (이 날의 핵심 박스 + 수업노트 박스 + 그룹 섹션들)
6. 라우팅 (`/` → 홈, `/day/[dayNum]` → Day)
7. 다크모드 토글

### Phase 2 — 노트 폴리시
8. 음성 재생 (예문 🔊, 그룹 🔊)
9. "이어서 보기" (lastViewedDay)
10. 이전/다음 Day 네비
11. 반응형 폴리시

### Phase 3 (나중)
12. Month 2 외 다른 월 추가 시 Month 선택 UI
13. 검색 (Day 내)
14. 폰트 크기 조절 (노트 앱스러운 옵션)

### Phase 4 — 연습 모드 (v2.1 추가)
15. Day 페이지에 `[📝 연습 모드]` 버튼 추가 (상단, 제목 옆)
16. `/day/[day]/practice` 라우트 생성
17. 연습 유형 3종 구현:
    - **빈칸 채우기**: 그룹의 동사를 가리고 자매 그룹 동사 3~4개 중 고르게
    - **동사 고르기**: 패턴(try N/~ing/to V 등)을 먼저 고르고 예문 공개
    - **자·타동사 분류**: Day 1 전용, 동사를 자/타/둘다로 분류
18. 세션 시작 전 선택 화면 (유형 + 길이: 5/10/전체)
19. 세션 진행 UI (문제 번호, 진행률, 정답 피드백, 자동 재생)
20. 세션 종료 요약 화면
21. 어려움 표시 기능:
    - `PracticeState.difficultExamples`에 저장
    - 노트 페이지에 조용한 점으로 표시
    - 다음 세션에서 우선 노출 옵션

---

## 8. 반응형

이 앱의 주 사용 환경은 **모바일 세로 모드**. 모바일 퍼스트로 설계.

### 모바일 (<768px, 기본)
- 컨테이너 `px-4 py-6`
- 홈: Day 카드 세로 스택, 한 카드 전체 너비
- Day 페이지: 한 컬럼, 텍스트 줄 길이 편하게
- 그룹 섹션 헤더는 세로 배치 (타이틀→패턴→의미)
- 예문 행도 세로 (영어→한국어)
- 🔊 버튼은 예문 아래 작게

### 태블릿 (md, ≥768px)
- 컨테이너 `px-6 py-8`
- 홈: Day 카드가 2열 그리드
- Day 페이지: 여전히 단일 컬럼 (노트처럼 읽기 편하게), 최대 너비 제한(`max-w-3xl`)

### 데스크탑 (lg, ≥1024px)
- 컨테이너 `px-8 py-10`, 최대 너비 `max-w-screen-xl mx-auto`
- Day 페이지는 `max-w-3xl` 중앙 정렬 (폭 너무 넓으면 읽기 불편)
- 예문 영어/한국어 `grid-cols-[1fr_1fr]`로 나란히

### 공통
- 폰트 크기는 본문 `text-base` 기준, 모바일에서도 작아지지 않게 (가독성 우선)
- 터치 타깃 최소 44×44px

---

## 부록: 파일 구성

```
english-learning-notebook/
├── README.md                    ← 이 문서 (기획)
├── CURSOR_PROMPT.md             ← Cursor Agent용 구현 지시서
├── DATA_SCHEMA.md               ← TypeScript 타입 정의
├── DESIGN_GUIDE.md              ← 디자인 가이드 (shadcn, 팔레트, 폰트, 컴포넌트 사용법)
├── CHANGELOG.md                 ← 이전 버전에서 바뀐 점 (Cursor가 이해하기 쉽게)
├── curriculum-m2.json           ← Month 2 데이터 (Day 요약 포함)
└── prototype/
    └── english_review.html      ← 이전 프로토타입 (참고용, 비주얼은 따르지 말 것)
```
