# Cloudflare Pages (Git 연동)
# 빌드 명령이 없으면 "Skipping build step" → out 폴더가 없어 404 남. 반드시 [build].command 필요.
name = "english"
compatibility_date = "2024-11-01"
pages_build_output_dir = "./out"

[build]
command = "npm run build"
cwd = "."
